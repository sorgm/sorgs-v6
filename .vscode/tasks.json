{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run http.server",
            "command": "python -m http.server",
            "type": "shell",
            "isBackground": true,
            "problemMatcher": "$jshint",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}